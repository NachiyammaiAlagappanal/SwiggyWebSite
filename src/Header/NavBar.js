const NavBar = () => {     
    return (
        <div className="navItems">
            <span>Home</span>
            <span>About Us</span>
            <span>Contact Us</span>
            <span>Cart</span>
        </div>
    );
};

export default NavBar;